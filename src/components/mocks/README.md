Mock product UIs used inside slides (a Linear issue, a GitHub PR, a browser
window, …). Keep them presentational and prop-driven so slides can drive them
from step state. See `sarah_aie_deck/src/components/mocks` for reference
implementations (BrowserWindow, MacCursor, DatadogDashboard).
