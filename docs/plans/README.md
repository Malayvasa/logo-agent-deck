# Slide design plans

One markdown file per slide that needs designing before it gets built, named
`YYYY-MM-DD-<slide>-design.md`. Same convention as `karan_aie_deck/docs/plans` —
sketch the beat, the layout, and the step-by-step reveal here, then implement it
as a component in `src/components/slides/`.
